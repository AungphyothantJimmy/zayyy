import { ComingSoonWithBack } from "@/components/ComingSoon";

export const metadata = { title: "Cart | Zayyy" };

export default function CartPage() {
  return (
    <ComingSoonWithBack
      title="Your cart is almost here"
      description="Add products from any shop and check out in one cart."
      phase="Phase 3"
    />
  );
}